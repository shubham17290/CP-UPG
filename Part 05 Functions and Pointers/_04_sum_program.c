#include <stdio.h>

int main() {
    int x, y, sum;

    printf("Enter value of x and y: ");
    scanf("%d %d", &x, &y);

    sum = x + y;
    printf("Thw sum value is : %d\n ", sum);

    return 0;
}
