
//  taking input from user in c programming language

#include <stdio.h>

int main() {
    float a, b, c;
    printf("Enter the value of a, b and c:\n");
    scanf("%f %f %f", &a, &b, &c);
    printf("Values entered are: %f, %f, %f\n", a, b, c);

    return 0;
}
